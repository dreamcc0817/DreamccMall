spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dreamcc0817/DreamccMall
          search-paths: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/
management:
  endpoints:
    web:
      expose: "*"